export type PodcastType = {
  id: string;
  name: string;
  author: string;
  image: string;
  description: string;
};

export type EpisodeType = {
  id: string;
  title: string;
  duration: string;
  description: string;
  url: string;
  date: string;
};
